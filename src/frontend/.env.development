# Loaded by `next dev`. Repo-root `.env` BACKEND_PROXY_URL wins when set via `make ui` / `make dev`.
# Keep in sync with BACKEND_PORT in repo-root `.env` (e.g. 8011 if 8010 is busy).
BACKEND_PROXY_URL=http://127.0.0.1:8011
NEXT_PUBLIC_DEV_MODE=true
